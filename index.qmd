---
title: "Untitled"
format: html
editor: visual
---

I wrote some code to explore the 'nycflights13' R dataset.

```{r}
library(tidyverse)
library(nycflights13)

flights |> 
  filter(dest == "IAH") |> 
  mutate(speed = distance / air_time * 60) |> 
  select(year:day, dep_time, carrier, flight, speed) |> 
  arrange(desc(speed))

fast_flights = flights |> 
  filter(dest == "IAH") |> 
  mutate(speed = distance / air_time * 60) |> 
  select(year:day, dep_time, carrier, flight, speed) |> 
  arrange(desc(speed))
```
